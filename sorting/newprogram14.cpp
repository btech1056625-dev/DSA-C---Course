#include<iostream>
#include<vector>
using namespace std;
int main(){
    vector<vector<int>> a;
    vector<int> v;
    v.push_back(1);
    v.push_back(2);
    v.push_back(3);
    a.push_back(v);
    


    return 0;
}