#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;
int main(){
    /*This algorithm uses Sieve, which is discussed above to find primes smaller than or equal to 
    (R)**1/2.  Our next question is why only up to R**(1/2)​, because non-prime numbers till range R, 
    will be marked as false by numbers smaller than R**(1/2) already.*/ 

    

    return 0;
}